# Connected research rehearsal

New research answers include structured HOLD/WIDEN/EXIT comparison candidates generated deterministically from the last tool evidence, alongside model prose. No regex extraction from prose, invented capital or automatic strategy choice. Candidates are saved inside the account-scoped research result; existing older answers remain unchanged. Missing/invalid ranges yield no invented candidates.

On the research page, WIDEN range cards have an inline local rehearsal. Explicit clicks restore the existing local lab, add synthetic funds, confirm a displayed mock policy, copy the range into a NEW mock-entry draft, confirm its exact version and execute. Position/job status and exit are on the same page. This is a synthetic entry using a comparison range, NOT an execution of a real WIDEN operation. Range endpoints are rounded outward to integer mock prices. The simulator remains opt-in, loopback-only, separate cookie ledger, and cannot execute real transactions. Its cookie state is shared by this browser's lab, not isolated by Privy account; no private answer text, user identity or token is copied into it. The browser-transferred range is deliberately labelled unverified input in the mock ledger.

Verification: real DeepSeek and Ethereum RPC returned all three structured candidates with signing disabled and Graph mock labelled. Initial longer live answer was rejected as incomplete; a bounded short-question retry passed. Browser fixture authentication/AI tested actual account-scoped persistence, reload, candidate card → fund → policy → draft → confirmation → mock entry → exit on desktop/mobile. Mobile fieldset overflow was reproduced and fixed. Browser checks are not real Privy login or chain execution acceptance.

Run with existing local configuration and ENABLE_SIMULATION_LAB=1, GRAPH_MODE=mock. Sign in using a separately approved operator identity; ask a new research question; open “Rehearse this range here — MOCK only” beneath WIDEN. If lab is disabled, the action fails rather than bypassing the gate. No server is left running by verification.

Remaining: real user login/allowlist acceptance, AI recommendation selection with explicit user objectives, trusted persisted research→real-operation proposal linkage and full version history, fork lifecycle, real constrained signing/settlement/reconciliation, and inherited SDK advisories. Pausing management is not withdrawing; recovery UI explains that a new explicit mock policy may be required before exit.
